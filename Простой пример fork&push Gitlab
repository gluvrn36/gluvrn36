Заходим в любой проект Gitlab и нажимаем Fork

Затем отключаем связь fork'а с родителем: меню слева - Settings -> General -> Advanced (жмём Expand) -> Remove fork relationship (потребуется подтверждение)

ssh git clone git@gitlab.url:<ваш номер логина>/cicd-app.git

Переносим в корень проекта файлы из одной из папок по выбору, например так:
cd cicd-app
mv go-app/* .
rm *-app -rf

Далее мы представляем, что это наше приложение. Пока что у нас нет CI/CD, но скоро появится :
git add .
git commit -m "Initial commit"
git push

Создаем ветку feature:
git checkout -b feature/test

Меняем что-нибудь (да хоть README):
vim README.md

Далее commit'им и push'им:
git commit -am "change important stuff"
git push origin feature/test

Создаём MR - можно пройти сразу по ссылке, которую вам выдаст Gitlab в терминал, а можно зайти в веб-интерфейс и выбрать в меню слева Merge Requests -> New merge request. Не забудьте проверить, что слияние будет из feature-ветки 
в master того же самого репозитория.

Можете познакомиться с интерфейсом, где можно комментировать и вообще взаимодействовать с коллегами, а также поддерживать (approve) и принимать MR. На стенде у вас коллег не будет, поэтому придется принять запрос самостоятельно 
нажатием на кнопку.

Далее можно присвоить этому commit'у в мастер новый tag. Это можно сделать не только через веб-интерфейс, но также и в терминале.

Для простоты заполним поля не уходя из браузера, делается это тут:

Меню слева Repository -> Tags -> New tag (кнопка справа вверху).

Если заполнить оба описания, то Gitlab создаст ещё и релиз для tag'а.
